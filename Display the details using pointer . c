"" CODE ""

  #include<stdio.h>
  struct student {
      char name[50];
      int rollnumber;
      char branch[50];
      float cgpa;
};

int main() {
  struct student s = {"Cherish" , 117 , "CSE CORE" , 9.4);
struct student*ptr =&s;
printf(" --- students details(using pointers) --- ");
printf(" Name : %s\n" , ptr->name);
printf(" Branch : %s\n" , ptr->branch);
printf(" RollNumber : %d\n" ,ptr->rollnumber);
printf("Cgpa: %.2f \n", ptr-> cgpa);
return 0;
 
}

  
